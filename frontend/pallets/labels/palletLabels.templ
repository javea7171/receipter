package labels

templ PalletLabelPage(palletID int64, barcode string, printedAt string) {
    <h1>Pallet Label</h1>
    <p>Pallet: { palletID }</p>
    <p>Barcode: { barcode }</p>
    <p>Printed: { printedAt }</p>
}
